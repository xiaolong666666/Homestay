import React from 'react'
import Signin from '../../component/sign'

document.title = "登录 - 恬逸小岛"

const Sign_in = (props) => {
    return (
        <Signin {...props} />
    );
};

export default Sign_in