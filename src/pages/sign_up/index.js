import React from 'react'
import Signup from '../../component/sign'

document.title = "注册 - 恬逸小岛"

const Sign_up = (props) => {
    return (
        <Signup {...props} />
    );
};

export default Sign_up